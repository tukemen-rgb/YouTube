# 辛口レビュー簿(自律改善ループの評価記録)

各サイクルで「辛口ユーザー」(動画を作りたい利用者視点)と
「辛口クリエイター」(YouTube 制作のプロ視点)が現状の成果物を評価し、
ここに追記する。対応したら状態を更新する(追記のみ・改ざんしない)。

状態: 未対応 / 対応中 / 対応済み(コミット) / 判断待ち(社長)

---

## サイクル 1 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U1 | analyze が長い動画で数分黙り込む。固まったのか進んでいるのか分からない。進み具合を出せ | 中 | 対応済み(サイクル 2) |
| U2 | cutplan.json を「手で直せ」と言うが、JSON を編集できるのはプログラマだけ。普通の人には無理 | 大 | 対応済み(サイクル 10、○×編集シート+apply) |
| U3 | インストールが Python + ffmpeg + フォント。動画編集したいだけの人はここで脱落する | 大 | 判断待ち(配布形態は方針判断) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C1 | **サムネイルが無い。** YouTube はサムネで再生が決まるのに、このパイプラインは mp4 を吐いて終わり。盛り上がり度を測っているなら、その最高点のフレームをサムネ候補として出すべき | 大 | 対応済み(サイクル 1) |
| C2 | テロップが 1 スタイル・画面下固定のみ。色・位置・サイズを場面で変えられないのは表現として貧しい | 中 | 対応済み(サイクル 12、telop_pos/telop_color) |
| C3 | 映像の場面転換がブツ切りのみ。軽いクロスフェードくらい選ばせろ | 中 | 対応済み(サイクル 8、暗転方式。重なり合成の真のクロスフェードは尺が変わるため必要時に別設計) |
| C4 | 1080p のレンダリングがシングルスレッドで遅い。決定性のためと言うが、プロは速度も要る。速さ優先モードが欲しい | 中 | 対応済み(サイクル 6) |

### サイクル 1 の選択

**C1(サムネイル候補の自動抽出)を実装する。** 理由: 盛り上がり度の
測定結果が既にあるので実装が小さく、8 段パイプラインの metadata 段の
最初の一歩になり、YouTube 運用上の価値が最も大きい。

---

## サイクル 2 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U4 | 長い動画だと analyze の区間一覧が何十行も流れて読めない。要約(切る合計・残す合計・上位3区間だけ)を先に出せ | 小 | 対応済み(サイクル 5) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C5 | サムネ候補が素のフレームだけ。文字入りサムネの土台(タイトル文字を重ねた版)も欲しい | 中 | 対応済み(サイクル 9) |
| C6 | 音量の正規化が無い。YouTube は実質 -14 LUFS 基準で、動画ごとに音量がバラつくチャンネルは素人くさい。書き出しでラウドネスを揃えろ | 中 | 対応済み(サイクル 3) |

### サイクル 2 の選択

**U1(analyze の進捗表示)を実装する。** 理由: 「固まったように見える」は
利用をやめる直接の理由になる。実装が小さく他機能を壊さない。C4(速度
モード)・C6(ラウドネス)は次サイクル以降の最有力。

---

## サイクル 3 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U5 | 計画を少し直して cut をやり直すたび全部を再エンコードして待たされる | 中 | 対応済み(サイクル 12、--incremental の差分再エンコード) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C7 | 16:9 の横動画しか作れない。いまの YouTube で伸ばすならショート(9:16 縦)対応は必須級 | 大 | 対応済み(サイクル 4) |

### サイクル 3 の選択

**C6(ラウドネス正規化)を実装する。** YouTube の実効基準(約 -14 LUFS)に
書き出しで揃える。1 パスの loudnorm は固定パラメータなら決定的で、
既存設計(同じ入力→同じ出力)を壊さない。C7(ショート)は規模が
大きいので次サイクルで設計から入る。

---

## サイクル 4 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U6 | ショートを作りたいのに「横動画を自分で縦に加工しろ」では本末転倒。1 コマンドで縦に出せるべき | 大 | 対応済み(サイクル 4) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C8 | 縦化は単純な中央クロップだと画面情報が欠ける。定番の「ぼかし背景+中央配置」で作るべき(ゲーム画面は特に UI が端にある) | 中 | 対応済み(サイクル 4) |

### サイクル 4 の選択

**C7/U6(ショート 9:16 出力)を実装する。** cut に --vertical を追加し、
連結後の映像を 1080x1920 へ「ぼかし背景+中央配置」で変換する。
--target-seconds 60 と組み合わせれば「録画→60秒ショート」が一本道になる。

---

## サイクル 5 — 2026-09-01(30 分進捗監査+U4)

### 進捗監査(30 分ごと)

- **前進の確認**: サイクル 1〜4 で C1(サムネ)・U1(進捗表示)・
  C6(ラウドネス)・C7/U6/C8(ショート)を消化。CI は 6 本連続 success。
  テストは 119 → 130 件に増加。停滞なし。
- **停滞要因の点検**: サイクル 3→4 の間隔が予定 5 分に対し約 50 分
  だった。原因は実行環境の再起動(セッション再開)で、こちらの作業
  内容の問題ではない。ループの状態をすべてリポジトリのファイルに
  置いてあるため、再開後も欠落なく続行できた(設計どおり)。対処:
  この方式を維持。間隔の揺れは許容する。
- **残る大物**: U2(JSON 手編集の難しさ)は対話型の計画編集が必要で
  規模が大きい。C4/U5(速度)・C2(テロップ様式)・C3(映像フェード)・
  C5(文字入りサムネ)が中粒で続く。

### サイクル 5 の選択

**U4(長い動画での出力要約)を実装する。** 監査と同サイクルで済む
小粒で、長時間録画を扱い始めた途端に効く。

---

## サイクル 6 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U7 | cut が終わった後の導線が薄い。サムネが出た後「次に何をすればよいか」を出せ | 小 | 対応済み(サイクル 9、cut 後に次の一手を案内) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C9 | BGM を重ねられない。ゲーム音だけの動画は間が持たない。手持ちの BGM ファイルをミックスできるべき(権利は利用者持ちの素材のみ) | 中 | 対応済み(サイクル 7) |

### サイクル 6 の選択

**C4/U5(レンダリング速度)を実装する。** cut に --fast を追加:
マルチスレッド+高速プリセットで待ち時間を削る。バイト単位の再現性は
--fast では保証しない(トレードオフを明示し、来歴に fast を記録)。
既定は従来どおり決定的なまま。

---

## サイクル 7 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U8 | BGM を付けたいだけなのに別の編集ソフトを開くのは二度手間 | 中 | 対応済み(サイクル 7) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C10 | BGM はゲーム音の「下」に置くのが基本。同音量で混ぜたら素人。既定は控えめ(-16dB 程度)で、末尾はフェードアウトすること | 中 | 対応済み(サイクル 7) |

### サイクル 7 の選択

**C9/U8(BGM ミックス)を実装する。** cut --bgm <ファイル> で手持ち音源を
重ねる(権利は利用者の素材のみ)。短ければループ、既定 -16dB、末尾
1.5 秒フェードアウト、ミックス後にラウドネス正規化、BGM の指紋を来歴に記録。

---

## サイクル 8 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U9 | 場面が変わったことに気づけない。切り替わりが分かる見た目の合図が欲しい | 中 | 対応済み(サイクル 8) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C11 | 場面転換は全部に同じ演出を入れると逆にうるさい。既定はハードカット、選んだときだけ軽い転換、が正しい | 小 | 対応済み(サイクル 8) |

### サイクル 8 の選択

**C3/U9(場面転換)を実装する。** cut --transition dip で各つなぎ目に
短い暗転(0.15 秒フェードアウト→フェードイン)を入れる。既定は
従来どおりハードカット(C11)。本物のクロスフェード(重なり合成)は
全体尺が変わり計画との対応が崩れるため、必要になった時に別途設計する。

---

## サイクル 9 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U10 | サムネに文字を入れるためだけに画像編集ソフトを開きたくない | 中 | 対応済み(サイクル 9) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C12 | サムネ文字は「大きく・少なく・縁取り」が鉄則。小さい字をたくさん載せるくらいなら無い方がまし | 小 | 対応済み(サイクル 9) |

### サイクル 9 の選択

**C5/U10(文字入りサムネ)を実装する。** thumbs コマンドを新設し、
--text でタイトル文字を重ねた版(縁取り+半透明帯、C12 の鉄則どおり
大きく)を候補ごとに出す。cut 後の案内文にも次の一手として載せ、
U7(導線)も一部解消する。

---

## サイクル 10 — 2026-09-01(30 分進捗監査+U2)

### 進捗監査(30 分ごと)

- **前進の確認**: サイクル 6〜9 で C4/U5(--fast 2.6 倍速)・C9/U8/C10
  (BGM)・C3/U9/C11(暗転つなぎ)・C5/U10/C12/U7(文字入りサムネと導線)
  を消化。CI は 11 本連続 success。テスト 133 → 150 件。停滞なし。
- **残る大物**: U2(JSON 手編集)のみ。本サイクルで対応。
- **U3(配布形態)は判断待ちのまま**(DECISIONS_PENDING D3)。

### サイクル 10 の選択

**U2(計画編集の敷居)を「○×編集シート」で解決する。** analyze が
cutplan.sheet.txt を書き、人は行頭の ○/× とテロップだけ書き換えて
apply コマンドで反映する。JSON の知識は不要になる。区間の切れ目の
変更だけは引き続き cutplan.json(必要になれば次の改善で)。

---

## サイクル 11 — 2026-09-01

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U11 | README が古い。ショート・BGM・シート編集など今の機能が書かれておらず、初見は何ができるのか分からない | 中 | 対応済み(サイクル 11) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C13 | 実機テスト手順書(RUNBOOK)が JSON 手編集前提のまま。帰国した社長が最初に読む文書が古いのは致命的 | 中 | 対応済み(サイクル 11) |

### サイクル 11 の選択

**U11/C13(文書の同期)を実施し v0.7.0 とする。** 残る改善候補は
C2(テロップ様式)と U5 の残り(差分再エンコード)のみで、いずれも
実データでの必要性確認が先。以降のサイクルは頻度を落とし、新規の
辛口評価で本当に価値のある指摘が出たときだけ実装する。

---

## サイクル 12 — 30 分周期の評価+監査

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C14 | --vertical のテロップが小さすぎる。テロップは 16:9 の絵に描いてから縦変換で縮むので、スマホの縦画面では読めない。ショートのテロップは横動画より「大きく」が常識 | 中 | 対応済み(サイクル 12) |

### 進捗監査

- サイクル 11 で v0.7.0(文書同期)。CI 緑継続。実装可能な既存指摘は
  全消化済みのため、本サイクルは新規評価から C14 を掘り起こして対応。

---

## サイクル 13 — 集中実装ウィンドウ

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U12 | 盛り上がり度が数字の羅列で直感的に分からない。どこが山でどこを切ったのか、絵で見せろ | 中 | 対応済み(サイクル 13) |

### サイクル 13 の選択

**U12(盛り上がり度グラフ)を実装する。** analyze のたびに
excitement.svg(点数の折れ線+切る区間の網掛け+★)を書き出す。
依存ゼロの自前 SVG 生成で、ブラウザで開くだけ。

---

## サイクル 14 — 集中実装ウィンドウ

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U13 | 切りすぎ・切らなすぎのとき、どのノブをどっちに回せばいいか分からない。AI 自身に「この結果は変では?」と気づいて提案させろ | 中 | 対応済み(サイクル 14) |

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C15 | 紹介動画(intro)が横だけ。新作告知こそショートで流すのが今の定石 | 小 | 対応済み(サイクル 14) |

### サイクル 14 の選択

**U13(analyze の自己診断)と C15(intro --vertical)を実装する。**
診断は純粋関数で「残し比率が極端」「音声なしで判定が落ちた」等を検知し、
具体的なノブ(--mode / --silence-db / --near-still-ydif)を提案する。

---

## サイクル 15 — 30 分監査+metadata 段の一歩目

### 進捗監査

- CI 16 本連続 success(17 本目実行中)。サイクル 12〜14 で C14/C2/U5/
  U12/U13/C15 を消化。テスト 150 → 168。停滞なし。

### 辛口クリエイター(作るプロ)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| C16 | チャプター(説明欄の 0:00 目次)が無い。区間とテロップを持っているのに手で書かせるのは無駄。説明文の下書きまで出せ | 中 | 対応済み(サイクル 15) |

### サイクル 15 の選択

**C16(チャプター+説明文の下書き)を実装する。** meta コマンドが
出力動画の時間軸でチャプター一覧と説明文の下書き(BGM クレジット込み)
を out/description.txt に書く。8 段パイプラインの metadata 段の一歩目。

---

## サイクル 16 — 集中実装ウィンドウ

### 辛口ユーザー(使う人)

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U14 | コマンドを 4 回打たないと一式そろわない(analyze→cut→thumbs→meta)。「まず全部見たい」の一発コマンドが無い | 中 | 対応済み(サイクル 16) |

### サイクル 16 の選択

**U14(auto コマンド)を実装する。** 録画 1 本から 分析→カット→サムネ→
説明文下書き まで一気に出す。--shorts で 60 秒縦ショートの一発生成にも
対応。auto は「AI の案のまま」なので、直すときはシート→apply→cut の
いつもの道へ誘導する(人が確定する原則は崩さない)。


### サイクル 16 の結果

auto コマンド実装完了。1 コマンドで 動画 / サムネ候補 / 説明文下書き /
盛り上がりグラフ / ○×シート が全部そろう。学習用の添削は auto では
**記録しない**(人の確認を経ていない案を「人の正解」として貯めると
採点の学習データが汚れるため。人が sheet→apply→cut で直したときだけ
cut 側が記録する)。e2e テスト追加でテスト 175 件。

### サイクル 16 精度実測(社長指示「テストして精度確認」)

正解入りの合成動画 24 秒(退屈 11 秒 / 見どころ 13 秒)で analyze を採点:

| 項目 | 既定モード(静止または無音を切る) | static_and_silent |
| --- | --- | --- |
| カット判定の一致 | 89.3% | **100%** |
| 切りすぎ | 1.8 秒(動きはあるが音が小さい場面) | 0 秒 |
| 切り残し | 0 秒 | 0 秒 |
| ★(一番の見どころ) | 正解(大音量の場面)に的中 | 同左 |
| 盛り上がり度の順位 | 大きい音 32 > ふつう 28(正解どおり) | — |

| # | 指摘 | 重さ | 状態 |
| --- | --- | --- | --- |
| U15 | 「動きはあるが音が小さい場面」(BGM 無しの操作シーン等)が既定モードでは無音扱いで切られる。切った理由の内訳(静止か無音か)を出し、無音側だけで切った時間が長いときは診断で static_and_silent を提案すべき | 中 | 未対応 |

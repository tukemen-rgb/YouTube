# 作業記録(追記のみ)

自律改善ループの前進の記録。約 30 分ごとの進捗監査はここを読んで
「前回から何が進んだか」を確かめ、停滞していれば原因と対処を書く。

---

- 2026-09-01 サイクル 1 開始。レビュー簿(REVIEWS.md)・この記録・
  判断待ち一覧(DECISIONS_PENDING.md)を新設。C1(サムネイル候補の
  自動抽出)の実装に着手。

- 2026-09-01 サイクル 1 完了。C1 対応: cut 後に盛り上がり度上位の瞬間を
  元動画から静止画で切り出す thumbs.py を追加(keep 区間内のみ・近接
  排除・選定理由を thumbnails.json に記録)。テスト 5 件追加(計 124)、
  lint 合格。次サイクルは U1(analyze の進捗表示)か C4(速さ優先
  モード)を評価から選ぶ。30 分監査はサイクル 6 前後で実施予定。

- 2026-09-01 サイクル 2 完了。U1 対応: analyze に工程の節目ごとの進捗
  表示を追加(確認→検出→動き測定→音量測定→採点→AI下書き、経過秒
  付き)。新規指摘 U4/C5/C6 をレビュー簿に起票。テスト計 125、lint 合格。
  次サイクル候補: C4(速さ優先モード)か C6(ラウドネス正規化)。

- 2026-09-01 サイクル 3 完了。C6 対応: cut の書き出しに 1 パス loudnorm
  (I=-14 / TP=-1.5 / LRA=11、既定オン、--no-loudnorm で無効)を追加し、
  YouTube の実効基準に音量を揃えた。新規指摘 U5(再カットの待ち時間)・
  C7(ショート 9:16 対応)を起票。テスト計 128、lint 合格。
  次サイクルは C7(ショート対応)の設計・実装に着手予定。

- 2026-09-01 サイクル 4 完了。C7/U6 対応: cut --vertical でショート用
  1080x1920 出力(ぼかし背景+中央配置)。60 秒超は注意表示。実レンダリング
  でも 1080x1920 になることを ffprobe で検証。テスト計 130、lint 合格。
  (訂正: サイクル 3 の記録でテスト計 128 としたが正しくは 127)
  次サイクルはサイクル 6 予定の 30 分監査を前倒しするか、C4(速度)か
  U4(出力の要約)を選ぶ。

- 2026-09-01 サイクル 5 完了(30 分監査+U4)。監査: サイクル 1〜4 の
  成果と CI 6 連続 success を確認、停滞なし。サイクル間隔の揺れは実行
  環境の再起動が原因で、リポジトリにループ状態を置く設計により無損失で
  再開できている。U4 対応: 区間が 12 を超えたら盛り上がり度上位 5 件の
  要約表示に切り替え。テスト計 133、lint 合格。

- 2026-09-01 サイクル 6 完了。C4/U5 対応: cut --fast(全コア+veryfast)。
  20 秒デモの実測で既定 5.1 秒 → 2.6 倍速の 2.0 秒(小さい動画での値。
  長尺・高解像度ほど差は開く)。既定は従来どおり決定的なまま、fast の
  使用は来歴 manifest に記録。新規指摘 U7(cut 後の導線)・C9(BGM
  ミックス)を起票。テスト計 134、lint 合格。次サイクル候補: C9(BGM)
  か C3(映像フェード)か U7(導線)。

- 2026-09-01 サイクル 7 完了。C9/U8/C10 対応: cut --bgm で手持ち音源を
  ゲーム音の下(既定 -16dB)に敷く。短ければループ・末尾 1.5 秒フェード
  アウト・ミックス後にラウドネス正規化・BGM の指紋と音量を来歴に記録。
  音声の無い動画では BGM が唯一の音声になる。実 ffmpeg での通し検証込みで
  テスト 3 件追加(計 137)、lint 合格。残る中粒: C2(テロップ様式)・
  C3(映像フェード)・C5(文字入りサムネ)・U7(導線)。大物: U2。

- 2026-09-01 サイクル 8 完了。C3/U9/C11 対応: cut --transition dip で
  つなぎ目に短い暗転(0.15 秒)。冒頭・末尾には演出を入れず、既定は
  ハードカットのまま。実 ffmpeg での通しも確認。テスト計 140、lint 合格。
  残り: C2(テロップ様式)・C5(文字入りサムネ)・U7(導線)・U2(大物)。

- 2026-09-01 サイクル 9 完了。C5/U10/C12 対応: thumbs コマンド新設。
  --text でタイトル文字入りサムネ(大きな字・縁取り・半透明帯、20 文字
  上限、textfile+expansion=none の安全経路)を候補ごとに生成。cut 後の
  案内に次の一手を追加し U7 も一部解消。実画像で見た目確認済み。
  テスト計 141、lint 合格。次サイクル(10)は 30 分監査+残件整理
  (U2 の設計検討を含む)。

- 2026-09-01 サイクル 10 完了(30 分監査+U2)。監査: サイクル 6〜9 の
  成果と CI 11 連続 success を確認、停滞なし。U2 対応: ○×編集シート
  (cutplan.sheet.txt)を analyze が出力し、apply コマンドで計画へ反映。
  行頭の ○/× とテロップだけで編集でき JSON 知識が不要に。未編集シートの
  往復同一・×にした区間のテロップ除去・番号不一致の拒否などテスト 9 件
  追加(計 150)、lint 合格。これで起票済みの大物・中粒はすべて消化。
  以降のサイクルは新しい辛口評価で掘り起こすか、実データ待ちの項目を
  文書化する段階に入る。

- 2026-09-01 サイクル 11 完了。U11/C13 対応: README とRUNBOOK を現機能
  (○×シート・apply・--vertical/--fast/--bgm/--transition・thumbs)に
  同期し v0.7.0。テスト計 150、lint 合格。起票済みの実装可能な指摘は
  すべて消化済み。残りは C2(テロップ様式)と U5 の残り(差分再エンコード)
  で、どちらも実録画での必要性確認が先(判断・実データ待ち)。以降は
  サイクル頻度を落とし、意味のある指摘が出たときだけ実装する方針
  (空回りの変更でコードを荒らさない)。

- サイクル 12 完了(社長の指示でトークンを惜しまない集中実装)。3 本を実装:
  (a) C14: --vertical のテロップを縮む分だけ大きく描き、スマホで読める
  サイズに(縦用は横用の 2 倍超)。
  (b) C2: 区間ごとの telop_pos(bottom/top)・telop_color(#RRGGBB)。
  (c) U5 完全対応: cut --incremental の差分再エンコード。区間を内容キー
  (元動画指紋+秒範囲+テロップ+フェード+設定)でキャッシュし、
  変わっていない区間は再利用。無変更の再カットは実測 3.3 倍速
  (20 秒デモ。区間が多い長尺ほど差は開く)。テロップ 1 区間の変更は
  その区間だけ再エンコードすることも実測で検証。--vertical とは併用不可
  (正直にエラー)。v0.8.0。テスト計 160、lint 合格。

- サイクル 13 完了。U12 対応: analyze が excitement.svg(点数の折れ線+
  切る区間の網掛け+★+テロップ札)を毎回書き出す。依存ゼロの自前 SVG
  生成、XML 妥当性と決定性をテストで検証。実デモでも生成確認。
  テスト計 162、lint 合格。

- サイクル 14 完了。U13 対応: analyze の自己診断(切りすぎ/切らなすぎ/
  音声なしの判定低下/細切れ keep を検知し、--mode・--silence-db・
  --near-still-ydif・--min-cut の具体的な調整案を表示。診断は助言で
  あって判断はしない)。C15 対応: intro --vertical でショート用の縦
  紹介動画。テスト計 168、lint 合格。

- サイクル 15 完了(30 分監査+C16)。監査: CI 16 連続 success、
  サイクル 12〜14 の成果確認、停滞なし。C16 対応: meta コマンドが
  出力動画の時間軸でチャプター(テロップ=見出し)と説明文の下書き
  (BGM クレジット行付き)を out/description.txt に生成。YouTube の
  目次条件(0:00 開始・3 個以上・各 10 秒以上)を満たさない場合は
  下書き内に注を明記。metadata 段が初めて done になる。
  テスト計 174、lint 合格。

## サイクル 16(2026-09-02)

- U14 対応: `videoyard auto` — 録画 1 本 → 分析 → カット → サムネ →
  説明文下書き の一気通貫コマンド。`--shorts` で 60 秒縦ショート一発生成。
- 設計判断: auto は学習用の添削を記録しない(未確認の AI 案を正解として
  学習させない)。理由は cli.py のコメントと REVIEWS.md に記載。
- e2e テスト追加(合成動画で auto 1 回 → 成果物 5 種と段の記録を確認)。
  テスト 174 → 175、ruff クリーン。

## サイクル 17(2026-09-02)

- U15/C17 対応: カット理由の内訳(静止画+無音/無音のみ/静止画のみ/吸収)。
  融合カットを検出境界で分割する split_cut を追加。diagnose が「無音のみ」
  カット過多のとき static_and_silent モードを提案。
- 精度実測の合成動画で提案が出ることを通しで確認。テスト 181 件、ruff クリーン。

## サイクル 18(2026-09-02)

- U16/C18 対応: README を v0.9 に同期(auto 一発の節を先頭に、meta /
  --incremental / 理由の内訳と診断提案を追記、「状態」欄の矛盾を解消)。
- バージョン 0.8.0 → 0.9.0。テスト 181 件、ruff クリーン。

## サイクル 19(2026-09-03)

- C19 対応: 精度ベンチマークを tests/test_accuracy.py として常設化。
  既定モード 85% 床+切り残しゼロ、and モード 100%、★的中、診断表示を
  リグレッションテスト化。テスト 184 件、ruff クリーン。

## サイクル 20(2026-09-03)

- U17 対応: auto --shorts の e2e テスト(出力寸法 1080x1920 を ffprobe 検証)。
  テスト 185 件、ruff クリーン。
- 社長指示「進捗確認とループの適正」に回答(チャット参照)。
